import { SolidButton } from '@/shared/components/Button/SolidButton';

//TODO: 견적 확정 버튼 클릭 api 연결
export const QuoteConfirmButton = () => {
  return <SolidButton text="견적 확정하기" onClick={() => {}} />;
};
